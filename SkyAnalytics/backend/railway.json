{
  "build": {
    "builder": "DOCKERFILE",
    "watchPatterns": ["SkyAnalytics/backend/**"]
  },
  "deploy": {
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
